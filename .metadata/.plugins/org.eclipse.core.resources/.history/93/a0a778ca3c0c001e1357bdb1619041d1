package datatype;

import java.math.BigInteger;

public class BIgNumberTest {

	/* ---------------- Big Numbers -------------------
	 * a) BigInteger
	 * b) BigDecimal
	 */
	
	public static void main(String[] args) {
	
		BigInterger a = BigInteger.valueOf(Long.MAX_VALUE); // to store the maximum value of long's or simple big numbers
		BigInterger b = BigInteger.valueOf(Long.MAX_VALUE);
		
		BigInteger c = (a + b);
		
	}
}
