package controlstatements;

public class ControlStmDocs {

	/*
	 * ------------------ Controls Statements-----------------------------
	 * # use to implement condition or logic in the programs
	 * 
	 * # Types :
	 * 1) Selection(Decesion Making)L
	 * 	a) if
	 * b) if else
	 * c) nested is else 
	 * d) switch
	 * 
	 * 
	 * 2) Looping(Iteration)
	 * a)for 
	 * -> enhance for loop/forEach loop'
	 * b) while
	 * c) do-while
	 * 
	 * 3) Jumping
	 * a) break
	 * b) continue
	 * c) break
	 */
}
